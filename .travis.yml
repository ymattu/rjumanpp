# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
addons:
  apt:
    packages:
    - gcc
    - libboost-dev
    - google-perftools
    - libgoogle-perftools-dev
install:
   - ./install-jumanpp.sh
